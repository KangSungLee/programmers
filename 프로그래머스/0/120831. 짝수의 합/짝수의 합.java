class Solution {
    public int solution(int n) {
        // int answer = 0;
        // for (int i = 2; i <= n; i += 2){
        //     answer += i;
        // }
        int answer = (n / 2 + 1) * (n / 2);
        return answer;
    }
}